// Глобальные константы приложения. Загружается ПЕРЕД auth.js / settings.js,
// чтобы YM_COUNTER и API_URL были доступны на этапе парсинга остальных файлов.
//
// Что класть сюда: значения, которые должны быть в одном экземпляре и используются
// из нескольких файлов (URL внешних API, ID счётчиков, feature flags и т.п.).
//
// Что НЕ класть сюда: константы, которые используются в одном файле — пусть живут
// рядом с использованием.

var YM_COUNTER = 108404687;
var API_URL = 'https://api.spring-tracker.ru:8080';
